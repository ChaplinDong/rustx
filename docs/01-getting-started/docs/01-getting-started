# 01 安装 Rust（全平台通用，详细实操版）
Rust 官方推荐使用 `rustup` 管理工具链（工具链包含：rustc 编译器、cargo 项目管理工具、rustup 工具链管理工具），支持 Windows、macOS、Linux 全平台，一键安装、自动配置，无需手动设置环境变量，新手可直接按照以下步骤操作。

## 一、Windows 系统（Windows 10/11 适用，32位/64位通用）
### 步骤1：下载安装程序
1.  打开电脑自带浏览器（Edge、Chrome 均可），访问 Rust 官方安装页面（若页面解析失败，可直接搜索“Rust 官方安装”找到对应页面）：https://www.rust-lang.org/tools/install
2.  页面加载完成后，找到“Download rustup-init.exe”按钮（通常在页面中间偏上位置，按钮为蓝色或绿色），点击下载
3.  下载过程中，建议保留默认下载路径（通常为“下载”文件夹），方便后续找到安装程序，下载完成后不要关闭浏览器（后续验证失败可再次访问）

### 步骤2：运行安装程序
1.  打开“下载”文件夹，找到刚刚下载的 `rustup-init.exe` 文件（图标为黑色命令行窗口样式）
2.  双击运行该文件，此时会弹出一个黑色的命令行窗口（请勿关闭，后续操作均在该窗口进行）
3.  命令行窗口会提示“Would you like to proceed with the installation?”（是否继续安装），直接按键盘上的「回车」键（默认选择 1，即“默认安装”）
4.  开始自动下载并安装 Rust 工具链，下载速度取决于网络情况，通常需要1-5分钟，期间命令行会显示下载进度（请勿关闭命令行窗口，否则安装会中断）

### 步骤3：验证安装是否成功
1.  安装完成后，命令行窗口会显示“Installation successful.”（安装成功）的提示，此时可关闭该命令行窗口
2.  重新打开一个新的命令行窗口（重要！必须重新打开，否则环境变量不生效）：
    - Windows 10/11：按下 Win + R 键，输入“cmd”，点击“确定”，即可打开命令行窗口
3.  在新打开的命令行窗口中，输入以下3条命令，分别验证三个核心工具是否安装成功（每条命令输入完成后按回车）：
    - 验证 rustc（编译器）：`rustc --version`
    - 验证 cargo（项目管理工具）：`cargo --version`
    - 验证 rustup（工具链管理工具）：`rustup --version`
4.  若每条命令都能显示对应的版本号（如 rustc 1.95.0 (abc12345 2026-03-04)），则说明安装成功；若提示“不是内部或外部命令”，请参考下文 04-common-issues.md 排查。

### 步骤4：Windows 系统额外注意事项
1.  若双击 `rustup-init.exe` 后，弹出“Windows 已保护你的电脑”提示，点击“更多信息”，再点击“仍要运行”即可（该提示是系统安全提示，rustup 是官方安全工具，可放心运行）
2.  若安装过程中提示“缺少 .NET Framework”，请按照提示下载并安装 .NET Framework 4.8 及以上版本，安装完成后重新运行 rustup-init.exe
3.  安装路径默认即可，无需修改，若手动修改路径，需记住路径，后续可能需要手动配置环境变量（新手不建议修改）

## 二、macOS 系统（macOS 10.13 及以上适用，Intel 芯片/Apple Silicon 芯片通用）
### 步骤1：打开终端
1.  打开 macOS 自带的“终端”应用：可通过 Launchpad 搜索“终端”，或在“应用程序→实用工具”中找到“终端”，双击打开
2.  打开后，终端窗口会显示当前用户路径（如 xxx@MacBook-Pro ~ %），确保终端处于正常运行状态（无报错提示）

### 步骤2：执行一键安装命令
1.  在终端中，复制以下完整命令（不要修改任何字符，包括符号），粘贴到终端中（粘贴快捷键：Command + V）：
    curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
2.  粘贴完成后，按回车执行命令，此时终端会开始下载安装脚本（脚本来自官方，安全可靠，可放心执行）
3.  脚本下载完成后，会提示“Would you like to proceed with the installation?”（是否继续安装），直接按键盘上的「回车」键（默认选择 1，即“默认安装”）
4.  开始自动下载并安装 Rust 工具链，下载速度取决于网络情况，通常需要1-5分钟，期间终端会显示下载进度（请勿关闭终端窗口，否则安装会中断）

### 步骤3：刷新环境变量并验证安装
1.  安装完成后，终端会显示“Installation successful.”（安装成功）的提示，此时需要刷新环境变量，才能让系统识别 Rust 相关命令
2.  在终端中输入以下命令，刷新环境变量（输入完成后按回车）：
    source $HOME/.cargo/env
3.  环境变量刷新完成后，输入以下3条命令，分别验证三个核心工具是否安装成功（每条命令输入完成后按回车）：
    - 验证 rustc（编译器）：`rustc --version`
    - 验证 cargo（项目管理工具）：`cargo --version`
    - 验证 rustup（工具链管理工具）：`rustup --version`
4.  若每条命令都能显示对应的版本号，则说明安装成功；若提示“command not found”（命令未找到），请参考下文 04-common-issues.md 排查。

### 步骤4：macOS 系统额外注意事项
1.  若执行安装命令时，提示“curl: command not found”，说明系统未安装 curl 工具，可通过以下命令安装：
    - 若已安装 Homebrew：`brew install curl`
    - 若未安装 Homebrew：打开浏览器访问 https://brew.sh/，按照页面提示安装 Homebrew 后，再执行 curl 安装命令
2.  若安装过程中提示“缺少 Xcode 命令行工具”，请按照提示点击“安装”，等待 Xcode 命令行工具安装完成后，重新执行 Rust 安装命令
3.  Apple Silicon 芯片（M1/M2/M3 系列）用户无需额外操作，安装脚本会自动识别芯片类型，安装对应的工具链
4.  安装路径默认即可，无需修改，安装完成后，Rust 相关工具会默认安装在 `~/.cargo` 目录下

## 三、Linux 系统（主流发行版适用，如 Ubuntu、CentOS、Fedora 等）
### 步骤1：打开终端
1.  打开 Linux 系统自带的终端（快捷键通常为 Ctrl + Alt + T），确保终端处于正常运行状态（无报错提示）
2.  若为服务器版 Linux（无图形界面），直接通过 SSH 连接后，即可进入终端

### 步骤2：执行一键安装命令
1.  在终端中，复制以下完整命令（不要修改任何字符，包括符号），粘贴到终端中（粘贴快捷键：Ctrl + Shift + V）：
    curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
2.  粘贴完成后，按回车执行命令，终端会开始下载安装脚本（脚本来自官方，安全可靠，可放心执行）
3.  脚本下载完成后，会提示“Would you like to proceed with the installation?”（是否继续安装），直接按键盘上的「回车」键（默认选择 1，即“默认安装”）
4.  开始自动下载并安装 Rust 工具链，下载速度取决于网络情况，通常需要1-5分钟，期间终端会显示下载进度（请勿关闭终端窗口，否则安装会中断）

### 步骤3：刷新环境变量并验证安装
1.  安装完成后，终端会显示“Installation successful.”（安装成功）的提示，此时需要刷新环境变量，才能让系统识别 Rust 相关命令
2.  在终端中输入以下命令，刷新环境变量（输入完成后按回车）：
    source $HOME/.cargo/env
3.  环境变量刷新完成后，输入以下3条命令，分别验证三个核心工具是否安装成功（每条命令输入完成后按回车）：
    - 验证 rustc（编译器）：`rustc --version`
    - 验证 cargo（项目管理工具）：`cargo --version`
    - 验证 rustup（工具链管理工具）：`rustup --version`
4.  若每条命令都能显示对应的版本号，则说明安装成功；若提示“command not found”（命令未找到），请参考下文 04-common-issues.md 排查。

### 步骤4：Linux 系统额外注意事项
1.  不同发行版的注意事项：
    - Ubuntu/Debian 系列：若执行安装命令时提示“缺少依赖”，可先执行 `sudo apt update && sudo apt install build-essential curl`，安装依赖后再重新执行安装命令
    - CentOS/Fedora 系列：若执行安装命令时提示“缺少依赖”，可先执行 `sudo dnf install gcc curl`，安装依赖后再重新执行安装命令
    - 其他发行版：若提示缺少依赖，可根据提示安装对应的 gcc、curl 等依赖工具
2.  非 root 用户也可正常安装，安装路径默认在 `~/.cargo` 目录下，无需 root 权限
3.  若安装过程中提示“/tmp 目录不可执行”，可参考安装脚本提示，将安装文件复制到可执行目录后再运行安装

## 四、通用关键说明（全平台适用）
1.  安装失败怎么办？
    - 首先检查网络是否稳定，网络不稳定会导致工具链下载中断，可重新执行安装命令
    - 若仍失败，参考下文 04-common-issues.md 中的“安装失败”相关问题，查找解决方案
    - 若所有方案都无法解决，可访问 Rust 中文社区（https://rustcc.cn/）提问，获取社区帮助
2.  如何更新 Rust 工具链？
    - 后续 Rust 发布新版本后，可在终端中输入以下命令，一键更新工具链：`rustup update`
    - 更新完成后，可通过 `rustc --version` 验证更新是否成功
3.  如何卸载 Rust？
    - 若后续需要卸载 Rust，可在终端中输入以下命令，一键卸载：`rustup self uninstall`
    - 卸载完成后，重新打开终端，输入 `rustc --version`，若提示“命令未找到”，则说明卸载成功
4.  安装脚本说明：
    - 本次使用的安装脚本（https://sh.rustup.rs）是 Rust 官方提供的 rustup 安装脚本，版本为 1.29.0，支持自动检测系统架构、自动下载对应版本的工具链
    - 脚本支持多种安装参数（如静默安装、指定工具链版本等），新手无需额外配置，默认安装即可
5.  环境变量说明：
    - 安装完成后，脚本会自动配置环境变量，将 Rust 相关工具的路径添加到系统 PATH 中
    - 若环境变量未生效，除了重新打开终端，也可手动执行 `source $HOME/.cargo/env` 命令刷新（Windows 系统无需手动执行，重新打开终端即可）
